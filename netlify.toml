[build]
  command = "npm run build"
  publish = "dist"

# Nitro's `netlify` preset emits the SSR function and its own redirects under
# `.netlify/`. No manual functions or redirects config is needed here.